<div class="d-flex justify-content-between mb-5">
  <h1>Recipes</h1>
  <div class="row align-items-center">
    <%= button_to "New recipe", new_recipe_path, method: :get, class: "btn btn-primary" %>
  </div>
</div>
<div id="recipes" class="row">
  <%= render @recipes %>
</div>
<%= paginate @recipes %>
